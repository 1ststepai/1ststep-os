# Os Update

## Purpose
Propose OS/schema/module change → compatibility analysis → version → migrate → verify → changelog.

## Requirement
Produce evidence, update canonical state, and create a handoff when the cycle materially changes project state.
