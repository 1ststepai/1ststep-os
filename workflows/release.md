# Release

## Purpose
Run gates → verify migrations/config/rollback/observability → deploy → production verification → post-release review.

## Requirement
Produce evidence, update canonical state, and create a handoff when the cycle materially changes project state.
